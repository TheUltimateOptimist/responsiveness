name: responsiveness
description: A minimal responsive framework for building UI for all screen sizes using the Bootstrap breakpoints.
version: 1.1.1
repository: https://github.com/TheUltimateOptimist/responsiveness

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
